//
//  Theme.swift
//  doctorhealth
//
//  Created by Gigi on 2025-07-05.
//
import SwiftUI

struct Theme {
    static let primaryColor = Color("TealBlue")
    static let secondaryColor = Color("DarkBlue")
    static let backgroundColor = Color.gray
    static let accentColor = Color("GreenAccent")
    static let purpleAccent = Color("PurpleAccent")
    static let whiteColor = Color.white

    static let titleFont = Font.system(size: 24, weight: .bold)
    static let bodyFont = Font.system(size: 16, weight: .medium)
    static let buttonFont = Font.system(size: 18, weight: .semibold)
}
